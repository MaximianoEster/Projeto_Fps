using System.Collections;
using System.Collections.Generic;
using Cinemachine;
using UnityEngine;

public class LookAround : MonoBehaviour
{
    [SerializeField] private CinemachineVirtualCamera _currentCamera = default;

    [SerializeField] private Transform _weapons = default; 
    // Update is called once per frame
    void Update()
    {
        
    }
}
